# Website

yeah, here is the code for the website



## Testing:

To test out site, either run golang program or use command below in project directory:

```
bundle exec jekyll serve
```

Also, you could just use `make` too which just runs the command above